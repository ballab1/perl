# core Packages

if [ "${CBF['os_id']}" = 'alpine' ]; then
    declare PKGS='ca-certificates curl findutils openssl shadow sudo tar wget zip'
    package.checkIfInstalled "$PKGS"
    apk add --no-cache $PKGS
fi