# build Packages

declare -a BUILD=( expat-dev
                   g++
                   gcc
                   gcc-objc
                   git
                   libtool
                   make )
declare -a PKGS=( expat
                  file
                  gnutls-utils
                  libc6-compat )
